import Foundation
import GenesisKit

// Prefer the GenesisKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = GenesisKit.AnyCodable
typealias InstanceIdentity = GenesisKit.InstanceIdentity
