apiVersion: v1
kind: Service
metadata:
  name: genesis
  labels:
    app: genesis
spec:
  type: ClusterIP
  selector:
    app: genesis
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
